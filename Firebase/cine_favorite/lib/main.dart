import 'package:flutter/material.dart';

void main() {

  @override
  Widget build(BuildContext context) {
    return const MaterialApp(
      home: Scaffold(
        body: Center(
          child: Text('Filmes'),
        ),
      ),
    );
  }
}

